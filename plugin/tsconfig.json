{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    // ES2019 matches @framelink/core's floor — this package bundles it into the sandbox, so its lib
    // must cover what core compiles against (Array.flat). A typings floor only: Figma's QuickJS
    // implements it, and get() on any text node exercises .flat() live.
    "lib": ["ES2019"],
    "types": ["@figma/plugin-typings"]
  },
  "include": ["src"],
  "//": "Tests are runtime-verified by tsx (node --test), not tsc: they use node:test + the figma mock, which this package's figma-only `types`/`lib` deliberately don't cover. tsx strips types at run time, so type-checking them here would only fight the restricted compile config.",
  "exclude": ["src/**/*.test.ts"]
}
