# Golden outputs are byte-exact captures of the tool's own serializer
# (src/tests/goldens/harness.ts). Prettier would re-wrap their arrays and break
# the byte-order gate, so it must never touch them.
src/tests/goldens/expected/

# Committed parity snapshots are regenerated by a script (JSON.stringify) and
# compared structurally, not by bytes. Keep Prettier off them so a regenerate
# run's output matches the committed files with no reformatting churn.
src/tests/parity/snapshots/
