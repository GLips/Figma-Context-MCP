{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    // Figma's QuickJS sandbox is the narrowest runtime this package ships into.
    "lib": ["ES2019"],
    // For tests/ only. src/ purity is enforced by the two gates there, not by this field.
    "types": ["node"]
  },
  "include": ["src", "tests"]
}
