# Dependencies
node_modules
.pnpm-store
package-lock.json

# Build output
dist

# Environment variables
.env
.env.local
.env.*.local

# IDE
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Testing
coverage
test-output

# OS
.DS_Store
Thumbs.db

# mcp-publisher CLI tool files
.mcpregistry*

# Private planning / brainstorm / ideas workspace — version-controlled in a
# separate private repo (FramelinkAI/mcp-docs) cloned to this path.
/docs/

# Local experiment scratchpad
/trials/

# External repos cloned for reference reading — never ours to commit
/.agent_cache/

.mcp.json
advisor-plans/
.tk
# Personal local helper scripts — untracked by design, listed per-file so new
# scripts/ files (CI-load-bearing ones like the flcm doc generator) commit by default.
/scripts/analyze-templates.ts
/scripts/debug-fetch.ts
/scripts/decompose.ts
/scripts/fetch-raw.ts
/scripts/measure-dedup.ts
/scripts/measure-format-default.ts
/scripts/perf-bench.ts

# Private Payload Lab captures and isolated replay runs
/.payload-lab/
